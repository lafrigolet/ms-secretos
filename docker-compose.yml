version: '3.9'

networks:
  sda-network:
    driver: bridge

services:

  # ── Nginx ────────────────────────────────────────────────────────
  nginx:
    image: nginx:alpine
    ports:
      - "${NGINX_PORT:-80}:80"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - auth-service
    networks:
      - sda-network
    restart: unless-stopped

  # ── Dev Stack Dashboard ─────────────────────────────────────────
  dev-stack:
    build:
      context: ./dev-stack
      dockerfile: Dockerfile
    expose:
      - "5174"
    ports:
      - "5174:5174"
    environment:
      - NODE_ENV=development
    volumes:
      - ./dev-stack/src:/app/src
    networks:
      - sda-network
    restart: unless-stopped

  # ── Frontend ─────────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    expose:
      - "5173"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
    volumes:
      - ./frontend/src:/app/src
    networks:
      - sda-network
    restart: unless-stopped

  # ── Subscription Service ─────────────────────────────────────────
  subscription-service:
    build:
      context: ./services/subscription-service
      dockerfile: Dockerfile
    expose:
      - "3017"
    ports:
      - "3017:3017"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3017
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - PAYMENT_MODE=${PAYMENT_MODE:-stub}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3017/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Sustainability Service ────────────────────────────────────────
  sustainability-service:
    build:
      context: ./services/sustainability-service
      dockerfile: Dockerfile
    expose:
      - "3016"
    ports:
      - "3016:3016"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3016
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3016/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Notification Preferences Service ────────────────────────────
  notification-preferences-service:
    build:
      context: ./services/notification-preferences-service
      dockerfile: Dockerfile
    expose:
      - "3015"
    ports:
      - "3015:3015"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3015
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3015/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Commercial Service ───────────────────────────────────────────
  commercial-service:
    build:
      context: ./services/commercial-service
      dockerfile: Dockerfile
    expose:
      - "3014"
    ports:
      - "3014:3014"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3014
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - SAP_INTEGRATION_URL=${SAP_INTEGRATION_SERVICE_URL:-http://sap-integration-service:3010}
    networks:
      - sda-network
    depends_on:
      - sap-integration-service
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3014/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Intelligence Service ─────────────────────────────────────────
  intelligence-service:
    build:
      context: ./services/intelligence-service
      dockerfile: Dockerfile
    expose:
      - "3013"
    ports:
      - "3013:3013"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3013
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - SAP_INTEGRATION_URL=${SAP_INTEGRATION_SERVICE_URL:-http://sap-integration-service:3010}
    networks:
      - sda-network
    depends_on:
      - sap-integration-service
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3013/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Content Service ──────────────────────────────────────────────
  content-service:
    build:
      context: ./services/content-service
      dockerfile: Dockerfile
    expose:
      - "3012"
    ports:
      - "3012:3012"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3012
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3012/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Returns Service ─────────────────────────────────────────────
  returns-service:
    build:
      context: ./services/returns-service
      dockerfile: Dockerfile
    expose:
      - "3011"
    ports:
      - "3011:3011"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3011
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - SAP_INTEGRATION_URL=${SAP_INTEGRATION_SERVICE_URL:-http://sap-integration-service:3010}
    networks:
      - sda-network
    depends_on:
      - sap-integration-service
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3011/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Auth Service ─────────────────────────────────────────────────
  auth-service:
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile
    expose:
      - "3001"
    ports:
      - "${AUTH_SERVICE_PORT:-3001}:3001"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3001
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN:-8h}
      - SAP_INTEGRATION_URL=${SAP_INTEGRATION_SERVICE_URL:-http://sap-integration-service:3010}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Catalog Service ──────────────────────────────────────────────
  catalog-service:
    build:
      context: ./services/catalog-service
      dockerfile: Dockerfile
    expose:
      - "3002"
    ports:
      - "${CATALOG_SERVICE_PORT:-3002}:3002"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3002
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - SAP_INTEGRATION_URL=${SAP_INTEGRATION_SERVICE_URL:-http://sap-integration-service:3010}
    networks:
      - sda-network
    depends_on:
      - sap-integration-service
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3002/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Customer Profile Service ─────────────────────────────────────
  customer-profile-service:
    build:
      context: ./services/customer-profile-service
      dockerfile: Dockerfile
    expose:
      - "3003"
    ports:
      - "${CUSTOMER_PROFILE_SERVICE_PORT:-3003}:3003"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3003
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - SAP_INTEGRATION_URL=${SAP_INTEGRATION_SERVICE_URL:-http://sap-integration-service:3010}
    networks:
      - sda-network
    depends_on:
      - sap-integration-service
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3003/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Promotions Service ───────────────────────────────────────────
  promotions-service:
    build:
      context: ./services/promotions-service
      dockerfile: Dockerfile
    expose:
      - "3004"
    ports:
      - "${PROMOTIONS_SERVICE_PORT:-3004}:3004"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3004
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3004/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Cart Service ─────────────────────────────────────────────────
  cart-service:
    build:
      context: ./services/cart-service
      dockerfile: Dockerfile
    expose:
      - "3005"
    ports:
      - "${CART_SERVICE_PORT:-3005}:3005"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3005
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - PROMOTIONS_SERVICE_URL=${PROMOTIONS_SERVICE_URL:-http://promotions-service:3004}
    networks:
      - sda-network
    depends_on:
      - promotions-service
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3005/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Order Service ────────────────────────────────────────────────
  order-service:
    build:
      context: ./services/order-service
      dockerfile: Dockerfile
    expose:
      - "3006"
    ports:
      - "${ORDER_SERVICE_PORT:-3006}:3006"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3006
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - SAP_INTEGRATION_URL=${SAP_INTEGRATION_SERVICE_URL:-http://sap-integration-service:3010}
      - NOTIFICATION_SERVICE_URL=${NOTIFICATION_SERVICE_URL:-http://notification-service:3007}
      - AUDIT_SERVICE_URL=${AUDIT_SERVICE_URL:-http://audit-service:3009}
    networks:
      - sda-network
    depends_on:
      - sap-integration-service
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3006/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Notification Service ─────────────────────────────────────────
  notification-service:
    build:
      context: ./services/notification-service
      dockerfile: Dockerfile
    expose:
      - "3007"
    ports:
      - "${NOTIFICATION_SERVICE_PORT:-3007}:3007"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3007
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3007/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Invoice Service ──────────────────────────────────────────────
  invoice-service:
    build:
      context: ./services/invoice-service
      dockerfile: Dockerfile
    expose:
      - "3008"
    ports:
      - "${INVOICE_SERVICE_PORT:-3008}:3008"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3008
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - SAP_INTEGRATION_URL=${SAP_INTEGRATION_SERVICE_URL:-http://sap-integration-service:3010}
    networks:
      - sda-network
    depends_on:
      - sap-integration-service
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3008/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Audit Service ────────────────────────────────────────────────
  audit-service:
    build:
      context: ./services/audit-service
      dockerfile: Dockerfile
    expose:
      - "3009"
    ports:
      - "${AUDIT_SERVICE_PORT:-3009}:3009"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3009
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3009/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── SAP Integration Service ──────────────────────────────────────
  sap-integration-service:
    build:
      context: ./services/sap-integration-service
      dockerfile: Dockerfile
    expose:
      - "3010"
    ports:
      - "${SAP_INTEGRATION_SERVICE_PORT:-3010}:3010"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3010
      - HOST=0.0.0.0
      - SAP_MODE=${SAP_MODE:-stub}
      - SAP_BASE_URL=${SAP_BASE_URL:-}
      - SAP_CLIENT=${SAP_CLIENT:-100}
      - SAP_USER=${SAP_USER:-}
      - SAP_PASSWORD=${SAP_PASSWORD:-}
      - SAP_LANGUAGE=${SAP_LANGUAGE:-ES}
      - CACHE_TTL_CUSTOMERS=${CACHE_TTL_CUSTOMERS:-300}
      - CACHE_TTL_PRODUCTS=${CACHE_TTL_PRODUCTS:-600}
      - CACHE_TTL_PRICES=${CACHE_TTL_PRICES:-300}
      - CACHE_TTL_STOCK=${CACHE_TTL_STOCK:-60}
    networks:
      - sda-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3010/health"]
      interval: 30s
      timeout: 5s
      retries: 3
